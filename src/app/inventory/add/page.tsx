import CardForm from "@/components/CardForm";

export default function AddCardPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center bg-gradient-to-br from-indigo-50 to-teal-100 py-8">
      <CardForm />
    </main>
  );
}